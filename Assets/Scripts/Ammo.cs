using UnityEngine;
using System.Collections;
using System;
using System.Linq;

public class Ammo : MonoBehaviour {

	// Use this for initialization
	void Start () {
        string keyString = "Ctrl+F";

        var ctrl = Enum.Parse(typeof(KeyCode), "F");
	}
	
	// Update is called once per frame
	void Update () {
        
	}
}
